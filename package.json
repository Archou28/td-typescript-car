{
  "name": "td-typescript-car",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && npx serve ."
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "serve": "^14.0.0"
  }
}
